'''
Challenge:
---------
Insert your code in each function below. You may create helper functions, but
DO NOT change the name of the given functions.

Testing:
-------
To test this script in the terminal, go to root directory
(programming_challenges/) and run `make test1`.
'''

def sort_elements(iterable):
    '''
    Sorts all the elements in a list in ascending order (without using other
    built-in sort functions or methods)

    Input
        iterable: LIST - a list of numeric values
    Output
        a sorted list of numeric values
    '''
    pass


def sort_unique(iterable):
    '''
    Sort all elements in a list and remove all redundant values (without using
    other built-in sort functions or methods)

    Input
        iterable: LIST - a list of numeric values
    Output
        a sorted list of numeric values
    '''
    pass


def sort_mixed_type(iterable):
    '''
    Sort a list that contains both numeric and string data types. If two
    element hold the same numeric value but are of different data types, the
    numeric data type should precede the string type.

    Input
        iterable: LIST - a list of string and numeric data types
            e.g. iter = ['4.15', 3, 507, '12', 4.15, 13.3]
    Output
        a sorted list; e.g. iter = [3, 4.15, '4.15', '12', 13.3, 507]
    '''
    pass


def is_palindrome(word):
    '''
    Detect whether word is a palidrome or not

    Input
        word: STR
    Output
        a boolean
    '''
    pass


def reverse(iterable):
    '''
    Reverses the order of either a list or a string (without using other
    built-in reverse functions or methods)

    Input
        iterable: LIST or STR
    Output
        a reversed list or string
    '''
    pass
